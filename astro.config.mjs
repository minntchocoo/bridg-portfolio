// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Deployed via GitHub Pages. Update `site` to the production URL.
  site: 'https://minntchocoo.github.io',
});
